import type { OpenSlideConfig } from '@open-slide/core';

const openSlideConfig: OpenSlideConfig = {
  // GitHub Pages project site: https://<user>.github.io/my-slide/
  // Every emitted asset URL is prefixed with this, so it must match the repo name.
  base: '/my-slide/',
};

export default openSlideConfig;
